# 项目优化第九阶段：Markdown 最终答案规范化（2026-07-21）

## 结论

用户以真实页面 DOM 复测“skill 是什么”及追问“和 mcp 有什么区别”后，确认前端 Markdown 解析器工作正常，但后端最终文本存在结构损坏：搜索过程话术与标题粘连，grounding 修复把表格行包装成列表项，导致浏览器只能渲染成普通段落或包含竖线文本的列表。

本阶段将 Markdown 保障从提示词要求升级为“语义补全、完整答案缓冲、确定性格式规范化、缓存失效和前端 DOM 清理”五层约束。没有调用 Claude 或外部模型 API。

## 1. 真实问题

附件中的实际输出包含：

```text
在知识库中搜索一下相关定义。### Skill 是什么
```

由于 `###` 不在行首，它不是 Markdown 标题。

比较回答包含连续的以下结构：

```text
- | 对比维度 | MCP | Agent Skills | [S1, S2]。
- | 核心价值 | 协议层标准化 | 应用层封装 | [S4]。
```

由于每个表格行前存在列表标记且缺少分隔行，GFM 会把它解析为普通列表，不会生成表格。

此外，`和mcp有什么区别` 省略了比较左侧对象。旧逻辑依赖模型从历史猜测 Skill，确定性完整性守卫无法验证回答是否同时覆盖双方。

## 2. 修复

- 新增最终答案 Markdown 规范化器；
- 删除确定属于过程说明的“搜索一下、检索一下”式开场；
- 将粘在句子后的 Markdown 标题移动到独立行；
- 修复结束标记前多余空格造成的粗体失效；
- 将连续 `- |...|` 行恢复为真正的 GFM 表格，并自动生成列数一致的分隔行；
- fenced code 内的 Markdown 示例保持原样，不参与改写；
- 定义、概览、详细说明和比较题等待完整生成后统一验证及规范化，避免已发送内容无法撤回；
- `和mcp有什么区别` 可根据上一轮“skill是什么”确定性补全为 `skill和mcp有什么区别`；
- 验证器忽略 Markdown 表头和分隔行，只校验有事实含义的数据行；
- 缓存版本提升为 `v7-markdown-normalized`，不复用旧的畸形格式答案；
- 清除 `react-markdown` 内部 `node` 对象被透传到 `<code>` 和链接 DOM 属性的问题。

## 3. 验证结果

| 检查项 | 结果 |
|---|---:|
| 真实失败形态的规范化回归 | 通过 |
| 后端相关定向测试 | 100/100 通过 |
| Agent 全量测试 | 319/319 通过 |
| Agent 与 Chat API 相关测试 | 331/331 通过 |
| 前端全量测试 | 67/67 通过 |
| Ruff | 通过 |
| Mypy | 通过 |
| Oxlint | 通过 |
| TypeScript + Vite 构建 | 通过 |
| 93×2 历史记录零调用重评分 | 质量门禁通过 |
| 独立发布门禁 | `passed: true`，无 blocker |

## 4. 复测要求

缓存版本已经变化，但 Docker 镜像必须重新构建才能包含本阶段代码。应使用新会话依次提问“skill是什么”和“和mcp有什么区别”。旧数据库中已经保存的历史回答不会被自动重写。

## 5. 同类风险扩展审计

继续审计所有 `answer_chunk`、缓存和流式发送路径后，又确认并处理以下同类风险：

- 工具调用前的“先搜索一下”曾提前作为 `answer_chunk` 进入浏览器；后端在 `tool_call` 时会清空，但前端没有同步清空，导致它粘在最终标题前。现在实时消息和历史消息都会隐藏工具协议 preamble；
- 整段答案偶尔被包在 ` ```markdown ` 中；现在验证前会安全解开只包裹整个答案的 Markdown 围栏；
- `###标题` 缺少标题空格；现在补为 `### 标题`；
- 普通表格可能缺少 GFM 分隔行；现在按实际列数补齐；
- 表格行可能被数字列表包装；现在与无序列表包装统一恢复为表格；
- “列出、总结、概括、安装步骤、使用指南、如何部署”等结构化问题可能逐句提前发送；现在等待完整答案后统一验证和格式化；
- 缓存命中路径增加防御性 Markdown 规范化，缓存版本继续提升为 `v8-markdown-hardened`；
- 术语型概览与普通短问句增加区分，避免为了格式化破坏既有流式来源校验行为。
- SSE 完成阶段新增 `answer_replace` 收口事件：无论回答来自普通流式、Web 工具还是其他非 RAG 路径，最终持久化和浏览器展示前都会再执行一次 Markdown 规范化；前端收到后原位替换，不会重复追加答案。

扩展回归覆盖有效表格不重复插入分隔行、代码围栏内示例不改写、真实步骤句不被误删，以及普通短问句不被错误分类为术语概览。
