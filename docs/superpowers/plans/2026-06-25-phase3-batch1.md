# 三期第一批 — 主题切换 + 检索去重 + Reranker 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 深灰蓝+蓝紫强调深色主题，米白暖色浅色主题，跨文档检索去重（保留最新），Cross-Encoder Reranker 精排。

**Architecture:** 前端 CSS 变量驱动双主题 + App.tsx 初始化 data-theme + Settings 加 toggle；后端 retriever.py RRF 融合后加去重步骤，再经新建的 reranker/ 包 Cross-Encoder 精排后返回。

**Tech Stack:** CSS custom properties, React, sentence-transformers, Python difflib

---

## 文件映射

| 文件 | 职责 | 类型 |
|------|------|------|
| `frontend/src/index.css` | CSS 变量重构 + 两套主题值 | 修改 |
| `frontend/src/App.tsx` | data-theme 初始化 | 修改 |
| `frontend/src/components/settings/SettingsPage.tsx` | 主题 toggle | 修改 |
| `backend/config.py` | dedup + reranker 配置项 | 修改 |
| `backend/reranker/base.py` | BaseReranker 抽象类 | 新建 |
| `backend/reranker/cross_encoder.py` | Cross-Encoder 实现 | 新建 |
| `backend/reranker/factory.py` | create_reranker() | 新建 |
| `backend/rag/retriever.py` | 去重 + Reranker 接入 | 修改 |

---

### Task 1: CSS 变量重构 + 双主题

**Files:**
- Modify: `frontend/src/index.css`

- [ ] **Step 1: 重构 CSS 为变量驱动 + 新增浅色主题**

将 `:root` 中的深色变量保留为默认，新增 `[data-theme="light"]` 选择器覆盖浅色值。

替换 `frontend/src/index.css` 的 `:root` 块（第 3-37 行）：

```css
:root {
  color-scheme: dark;

  /* ── 深色主题 (默认) ── */
  --bg:            #0f172a;
  --surface:       #1e293b;
  --surface-hover: #273449;
  --fg:            #e2e8f0;
  --muted:         #94a3b8;
  --border:        #334155;
  --border-light:  #475569;

  --accent:        #818cf8;
  --accent-hover:  #a5b4fc;
  --accent-dim:    rgba(129, 140, 248, 0.10);
  --accent-soft:   rgba(129, 140, 248, 0.18);

  --success:       #34d399;
  --success-dim:   rgba(52, 211, 153, 0.10);
  --warn:          #fbbf24;
  --warn-dim:      rgba(251, 191, 36, 0.12);
  --danger:        #f87171;
  --danger-dim:    rgba(248, 113, 113, 0.10);

  --radius-sm:     5px;
  --radius:        7px;
  --radius-lg:     9px;

  --font-sans:     'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
  --font-mono:     'JetBrains Mono', 'IBM Plex Mono', ui-monospace, Menlo, monospace;

  --shadow-sm:     0 1px 2px rgba(0,0,0,0.3);
  --shadow:        0 4px 16px rgba(0,0,0,0.25);
  --shadow-lg:     0 12px 40px rgba(0,0,0,0.4);
}

[data-theme="light"] {
  color-scheme: light;

  --bg:            #faf7f2;
  --surface:       #ffffff;
  --surface-hover: #f5f0e8;
  --fg:            #3d3929;
  --muted:         #8b8474;
  --border:        #e8e0d0;
  --border-light:  #f0ead8;

  --accent:        #6366f1;
  --accent-hover:  #4f46e5;
  --accent-dim:    rgba(99, 102, 241, 0.08);
  --accent-soft:   rgba(99, 102, 241, 0.15);

  --success:       #059669;
  --success-dim:   rgba(5, 150, 105, 0.08);
  --warn:          #d97706;
  --warn-dim:      rgba(217, 119, 6, 0.08);
  --danger:        #dc2626;
  --danger-dim:    rgba(220, 38, 38, 0.08);

  --shadow-sm:     0 1px 3px rgba(0,0,0,0.06);
  --shadow:        0 4px 20px rgba(0,0,0,0.06);
  --shadow-lg:     0 12px 40px rgba(0,0,0,0.08);
}
```

注意：`surface` 从 `#13141f` 改为 `#1e293b`，`accent` 从 `#2dd4a8` 改为 `#818cf8`（蓝紫）。

- [ ] **Step 2: 验证构建通过**

```bash
cd D:/Python/subject1/RAG_Agent/frontend && npx tsc --noEmit
```
Expected: 零错误

---

### Task 2: App.tsx 主题初始化

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: 添加 useEffect 初始化 data-theme**

在 `App` 组件开头添加主题初始化逻辑：

```tsx
import { useEffect } from "react";

export default function App() {
  useEffect(() => {
    const saved = localStorage.getItem("rag_agent_theme");
    if (saved === "light" || saved === "dark") {
      document.documentElement.setAttribute("data-theme", saved);
    } else {
      // Follow system preference
      const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
      document.documentElement.setAttribute("data-theme", prefersDark ? "dark" : "light");
    }
  }, []);

  return (
    <ConfirmProvider>
      {/* ... rest unchanged ... */}
    </ConfirmProvider>
  );
}
```

完整文件内容：

```tsx
import { useEffect } from "react";
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { MainLayout } from "./components/layout/MainLayout";
import { ChatPanel } from "./components/chat/ChatPanel";
import { DocumentList } from "./components/documents/DocumentList";
import { SettingsPage } from "./components/settings/SettingsPage";
import { ToastContainer } from "./components/shared/Toast";
import { ConfirmProvider } from "./components/shared/ConfirmDialog";

export default function App() {
  useEffect(() => {
    const saved = localStorage.getItem("rag_agent_theme");
    if (saved === "light" || saved === "dark") {
      document.documentElement.setAttribute("data-theme", saved);
    } else {
      const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
      document.documentElement.setAttribute("data-theme", prefersDark ? "dark" : "light");
    }
  }, []);

  return (
    <ConfirmProvider>
      <BrowserRouter>
        <Routes>
          <Route element={<MainLayout />}>
            <Route path="/" element={<ChatPanel />} />
            <Route path="/documents" element={<DocumentList />} />
            <Route path="/settings" element={<SettingsPage />} />
          </Route>
        </Routes>
      </BrowserRouter>
      <ToastContainer />
    </ConfirmProvider>
  );
}
```

- [ ] **Step 2: 验证构建通过**

```bash
cd D:/Python/subject1/RAG_Agent/frontend && npx tsc --noEmit
```

---

### Task 3: 设置页主题 Toggle

**Files:**
- Modify: `frontend/src/components/settings/SettingsPage.tsx`

- [ ] **Step 1: 添加主题状态和切换逻辑**

在 `SettingsPage` 组件中，`saved` state 之后添加主题状态：

```tsx
const [theme, setTheme] = useState<"system" | "dark" | "light">(() => {
  const t = localStorage.getItem("rag_agent_theme");
  return t === "dark" || t === "light" ? t : "system";
});
```

添加切换函数：

```tsx
const handleThemeChange = (val: "system" | "dark" | "light") => {
  setTheme(val);
  if (val === "system") {
    localStorage.removeItem("rag_agent_theme");
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
    document.documentElement.setAttribute("data-theme", prefersDark ? "dark" : "light");
  } else {
    localStorage.setItem("rag_agent_theme", val);
    document.documentElement.setAttribute("data-theme", val);
  }
};
```

- [ ] **Step 2: 在保存按钮之前添加主题选择区域**

在 `{/* ── Save ── */}` 注释之前插入：

```tsx
        {/* ── Theme ── */}
        <div className="settings-section">
          <div className="settings-section-title">主题外观</div>
          <div className="settings-field">
            <label>主题模式</label>
            <select value={theme} onChange={(e) => handleThemeChange(e.target.value as "system" | "dark" | "light")}>
              <option value="system">跟随系统</option>
              <option value="dark">深色</option>
              <option value="light">浅色</option>
            </select>
          </div>
        </div>
```

- [ ] **Step 3: 验证构建通过**

```bash
cd D:/Python/subject1/RAG_Agent/frontend && npx tsc --noEmit
```

---

### Task 4: 后端配置项

**Files:**
- Modify: `backend/config.py`

- [ ] **Step 1: 添加 Reranker 和去重配置**

在 `Settings` 类中，`retrieval_top_k` 之后添加：

```python
    # Retrieval dedup
    dedup_enabled: bool = True
    dedup_similarity_threshold: float = 0.85

    # Reranker
    rerank_enabled: bool = True
    rerank_model: str = "BAAI/bge-reranker-v2-m3"
    rerank_top_n: int = 10
```

- [ ] **Step 2: 验证导入**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -c "from config import settings; print(settings.rerank_enabled, settings.dedup_similarity_threshold)"
```
Expected: `True 0.85`

---

### Task 5: Reranker 包

**Files:**
- Create: `backend/reranker/__init__.py`
- Create: `backend/reranker/base.py`
- Create: `backend/reranker/cross_encoder.py`
- Create: `backend/reranker/factory.py`

- [ ] **Step 1: 创建 base.py**

```python
from abc import ABC, abstractmethod


class BaseReranker(ABC):
    @abstractmethod
    def rerank(self, query: str, documents: list[str]) -> list[tuple[int, float]]:
        """Rerank documents against query.

        Returns list of (original_index, score) sorted by score descending.
        """
        ...
```

- [ ] **Step 2: 创建 cross_encoder.py**

```python
from sentence_transformers import CrossEncoder
from .base import BaseReranker


class CrossEncoderReranker(BaseReranker):
    def __init__(self, model_name: str):
        self.model = CrossEncoder(model_name)

    def rerank(self, query: str, documents: list[str]) -> list[tuple[int, float]]:
        if not documents:
            return []
        pairs = [[query, doc] for doc in documents]
        scores = self.model.predict(pairs)
        # scores is a list of floats
        indexed = [(i, float(scores[i])) for i in range(len(documents))]
        indexed.sort(key=lambda x: x[1], reverse=True)
        return indexed
```

- [ ] **Step 3: 创建 factory.py**

```python
from config import settings
from .base import BaseReranker
from .cross_encoder import CrossEncoderReranker

_reranker: BaseReranker | None = None


def create_reranker() -> BaseReranker | None:
    global _reranker
    if not settings.rerank_enabled:
        return None
    if _reranker is None:
        _reranker = CrossEncoderReranker(settings.rerank_model)
    return _reranker
```

- [ ] **Step 4: 创建 __init__.py**

空文件。

- [ ] **Step 5: 安装依赖**

```bash
cd D:/Python/subject1/RAG_Agent/backend && pip install sentence-transformers
```

- [ ] **Step 6: 验证导入**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -c "from reranker.factory import create_reranker; print('Reranker package OK')"
```

---

### Task 6: Reranker 测试

**Files:**
- Create: `backend/tests/reranker/__init__.py`
- Create: `backend/tests/reranker/test_reranker.py`

- [ ] **Step 1: 编写测试**

```python
import pytest
from reranker.base import BaseReranker


class DummyReranker(BaseReranker):
    """Always returns documents in the same order with fake scores."""
    def rerank(self, query: str, documents: list[str]) -> list[tuple[int, float]]:
        return [(i, 1.0 - i * 0.1) for i in range(len(documents))]


class TestBaseReranker:
    def test_abstract_interface(self):
        reranker = DummyReranker()
        results = reranker.rerank("test query", ["doc a", "doc b", "doc c"])
        assert len(results) == 3
        assert results[0] == (0, 1.0)
        assert results[1] == (1, 0.9)
        assert results[2] == (2, 0.8)

    def test_empty_documents(self):
        reranker = DummyReranker()
        results = reranker.rerank("query", [])
        assert results == []


class TestCrossEncoderReranker:
    @pytest.mark.slow
    def test_real_reranker(self):
        try:
            from reranker.cross_encoder import CrossEncoderReranker
            reranker = CrossEncoderReranker("BAAI/bge-reranker-v2-m3")
            docs = [
                "Python is a programming language",
                "The weather is nice today",
                "Python is used for data science",
            ]
            results = reranker.rerank("What is Python?", docs)
            assert len(results) == 3
            # doc 0 and doc 2 should score higher than doc 1
            scores = {i: s for i, s in results}
            assert scores[0] > scores[1] or scores[2] > scores[1]
        except Exception as e:
            pytest.skip(f"Reranker model not available: {e}")
```

- [ ] **Step 2: 运行基础测试**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -m pytest tests/reranker/test_reranker.py::TestBaseReranker -v
```
Expected: 2 passed

---

### Task 7: Retriever 去重 + Reranker 接入

**Files:**
- Modify: `backend/rag/retriever.py`

- [ ] **Step 1: 添加去重和 Reranker 逻辑**

在 `_rrf_fusion` 返回后，`hybrid_search` 返回前，插入去重和 Reranker 步骤。

修改 `hybrid_search` 函数，在调用 `_rrf_fusion` 之后加：

```python
async def hybrid_search(query: str, top_k: int | None = None, use_rerank: bool = False) -> list[RetrievalResult]:
    if top_k is None:
        top_k = settings.retrieval_top_k

    embedding = create_embedding()
    vectordb = await create_vectordb()
    fts = SQLiteFTS5()

    query_vector = await embedding.embed_query(query)
    vector_results, text_results = await asyncio.gather(
        vectordb.search(query_vector, top_k=top_k * RRF_CANDIDATE_MULTIPLIER),
        fts.search(query, top_k=top_k * RRF_CANDIDATE_MULTIPLIER),
    )

    # RRF fusion
    results = _rrf_fusion(vector_results, text_results, k=top_k * 3)

    # Dedup: remove near-duplicate chunks from different docs, keep newest
    if settings.dedup_enabled:
        results = await _dedup_results(results)

    # Reranker: Cross-Encoder re-scoring
    if use_rerank and settings.rerank_enabled:
        results = await _rerank_results(query, results, top_k)

    return results[:top_k]
```

同时添加两个辅助函数（放在 `_rrf_fusion` 之后）：

```python
import difflib
from datetime import datetime, timezone
from models.database import async_session
from models.schemas import Document
from sqlalchemy import select


async def _dedup_results(results: list[RetrievalResult]) -> list[RetrievalResult]:
    """Remove near-duplicate chunks from different documents, keeping newest doc."""
    if len(results) <= 1:
        return results

    # Load document created_at for comparison
    doc_ids = list({r.document_id for r in results})
    async with async_session() as session:
        result = await session.execute(
            select(Document.id, Document.created_at).where(Document.id.in_(doc_ids))
        )
        doc_times: dict[str, datetime] = {row[0]: row[1] for row in result.all()}

    threshold = settings.dedup_similarity_threshold
    kept: list[RetrievalResult] = []

    for r in results:
        replaced = False
        for i, existing in enumerate(kept):
            if r.document_id != existing.document_id:
                ratio = difflib.SequenceMatcher(None, r.text, existing.text).ratio()
                if ratio >= threshold:
                    r_time = doc_times.get(r.document_id)
                    e_time = doc_times.get(existing.document_id)
                    if r_time and e_time and r_time > e_time:
                        kept[i] = r  # Replace with newer doc's chunk
                    replaced = True
                    break
        if not replaced:
            kept.append(r)

    return kept


async def _rerank_results(
    query: str, results: list[RetrievalResult], top_k: int
) -> list[RetrievalResult]:
    """Re-rank results using Cross-Encoder."""
    from reranker.factory import create_reranker

    reranker = create_reranker()
    if reranker is None or len(results) <= 1:
        return results

    # Take top N candidates for reranking
    candidates = results[: settings.rerank_top_n]
    texts = [r.text for r in candidates]
    ranked = reranker.rerank(query, texts)

    reranked = [candidates[i] for i, _ in ranked[:top_k]]
    # Append any remaining candidates beyond rerank_top_n
    if len(results) > settings.rerank_top_n:
        reranked.extend(results[settings.rerank_top_n:])
    return reranked
```

- [ ] **Step 2: 验证导入**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -c "from rag.retriever import hybrid_search; print('Retriever OK')"
```

- [ ] **Step 3: 运行现有检索测试**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -m pytest tests/rag/test_retriever.py -v
```
Expected: 所有 RRF 相关测试仍 PASS

---

### Task 8: 全量回归

- [ ] **Step 1: 运行全部测试**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -m pytest tests/ -v --ignore=tests/vectordb
```
（跳过 pre-existing Qdrant 维度问题的 test_upsert_and_search）

Expected: 所有测试 PASS（除了 pre-existing Qdrant 维度问题）

- [ ] **Step 2: 前端构建验证**

```bash
cd D:/Python/subject1/RAG_Agent/frontend && npx tsc --noEmit
```
Expected: 零错误

- [ ] **Step 3: 后端导入验证**

```bash
cd D:/Python/subject1/RAG_Agent/backend && python -c "
from config import settings
from rag.retriever import hybrid_search
from reranker.factory import create_reranker
print('All imports OK')
print(f'Dedup: {settings.dedup_enabled}, threshold={settings.dedup_similarity_threshold}')
print(f'Rerank: {settings.rerank_enabled}, model={settings.rerank_model}')
"
```
