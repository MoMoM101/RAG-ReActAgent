# 安装 OCR 可选依赖（docTR 引擎）:
# pip install -r requirements-ocr.txt
#
# docTR 使用 PyTorch，模型首次运行时会自动下载。
# 国内用户建议设置 HF_ENDPOINT=https://hf-mirror.com

python-doctr>=0.9,<1
torch>=2.0
opencv-python-headless>=4.5
